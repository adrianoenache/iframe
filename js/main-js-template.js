// components
// import { pwa } from 'pwa'

// this function will group all code to be initialized
const initCode = () => {

    // starting components
    // pwa('serviceWorker.js')

}

// all code will be executed after page is load
window.addEventListener('load', () => {

  initCode()

})
